# ThreeJS Globe Render Application

## Development server setup

Run `npm run dev` for a dev Vite server. Then, navigate to `http://localhost:3000/`.

Earth image link: http://textures.forrest.cz/index.php?spgmGal=maps